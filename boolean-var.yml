---
 - name: Boolean Variable in Ansible
   hosts: webservers
   become: yes
   vars:
    is_enabled: true
   tasks:    
     - name: Printing Boolena Variable Value
       debug:
        msg: "{{ is_enabled }}"
     - name: Printing Value in vars
       debug:
         var: is_enabled